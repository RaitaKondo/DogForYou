import * as React from "react"
import { Link } from "gatsby"

const Header = ({ siteTitle }) => (
  <header>
    <div className="header1">
      <h1>Welcome to Dog For You!</h1>
    </div>
  </header>
)

export default Header
